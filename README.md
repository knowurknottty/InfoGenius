# InfoGenius Evidence Studio

InfoGenius is being reconstructed as a data-first evidence-to-visual-story compiler.

Authority flow: `source → deterministic parse/profile → evidence → claim → VisualSpec → code renderer → verification → export`.

The repository was empty at reconstruction start on 2026-08-19. Historical implementation behavior is therefore treated as a documented compatibility requirement, not as observed repository truth.
